# AGENTS.md

> Generated by [ai-ship](https://github.com/G12789/ai-ship). Edit freely.

## AI Ship Skills

本项目安装了以下 Agent Skills（`SKILL.md` 工作流 + 可执行 scripts）：

- **session-start** — 会话起手（每天 / 每个新 AI 会话）
- **vision-auto** — 自动看图（贴截图 / DeepSeek 文本模型时）
- **prompt-guard** — Prompt 防退化（改 prompt / 模板时）
- **api-bridge** — API → MCP（接 REST API 时）
- **ship-check** — 发货检查（提交 / PR 前）

## 手动 CLI（不通过 Agent 时）

```bash
npx ai-ship ctx          # 打包会话上下文
npx ai-ship eval         # prompt 回归测试
npx ai-ship mcp <name>   # 生成 MCP Server
npx ai-ship check        # 提交前检查
```

## 验证改动

改完代码后跑项目自己的 test/lint；改 prompt 后跑 `npx evaldrift run`。

## 项目说明

<!-- 在 ai-ship install 生成 AGENTS.md 时，此文件内容会追加到「项目说明」章节 -->
<!-- 请在此填写：技术栈、目录约定、测试命令、部署方式等 -->
